<Window x:Class="DiliveryApp2._0.View.Windows.DynamicWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DiliveryApp2._0.View.Windows"
        mc:Ignorable="d"
        Title="DynamicWindow" Height="450" Width="800">
    <!--<Grid>
        --><!--<DataGrid SelectedItem="{Binding SelectedCustomer}"
                  ItemsSource="{Binding Customers}"
                  IsReadOnly="True"
                  AutoGenerateColumns="True" />-->
        
        <!--<ListView ItemsSource="{Binding TableData}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Table Name" DisplayMemberBinding="{Binding TableName}" />
                    <GridViewColumn Header="Data" DisplayMemberBinding="{Binding Data}" />
                </GridView>
            </ListView.View>
        </ListView>--><!--
    </Grid>-->
    <Grid>
        <TabControl x:Name="tabControl" />

        <Button Content="Load Tables" 
                HorizontalAlignment="Right" 
                VerticalAlignment="Top" 
                Click="LoadTablesButton_Click" 
                Margin="10"/>
    </Grid>
</Window>
